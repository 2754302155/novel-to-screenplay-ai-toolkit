# AI 长文本剧本生成优化设计

## 背景

当前真实 AI 接入后，短文本可以完成剧本 YAML 初稿生成，但当输入小说正文较长时，例如项目根目录 `test.txt` 约 2.6 万字，系统会将完整正文一次性发送给 OpenAI 兼容接口。使用 DashScope `deepseek-v4-pro` 时，服务端需要处理大输入并生成大体量 YAML，容易在返回响应头前超过客户端超时时间，出现：

```text
Client.Timeout exceeded while awaiting headers
```

根因不是单纯超时时间过短，而是“单次全量生成”把输入、推理和输出压力集中到了一个请求中。

## 目标

- 支持 2 万字以上小说正文稳定生成可编辑 YAML 初稿。
- 避免单个 AI 请求过大，降低超时概率。
- 任务进度可观测，用户能看到正在处理的文本块。
- 某个文本块失败时不拖垮整个任务，允许降级生成占位内容并标记人工复核。
- 最终 YAML 由后端合并和修复，保证结构稳定。

## 非目标

- 本阶段不引入外部队列系统。
- 本阶段不把任务持久化到 PostgreSQL；任务仍沿用现有内存仓库。
- 本阶段不做多模型调度和并发加速，优先保证稳定。

## 设计方案

### 1. 章节正文保留

章节解析结果已经包含 `body` 字段。创建转换任务时，后端保留章节正文，用于后续按章拆分。为了避免最终 YAML 泄漏大段正文，`body` 只参与任务内部处理，不写入 YAML。

### 2. 文本分块

生成任务启动后，后端将输入拆成多个文本块：

- 优先按章节拆分。
- 单章超过字符阈值时继续按段落切分。
- 默认单块最大字符数建议为 6000。
- 如果章节缺少正文，则回退使用完整 `source_text` 按字符切分。

每个文本块记录：

- 块序号
- 总块数
- 来源章节 ID
- 来源章节标题
- 块正文

### 3. 逐块 AI 生成

每次只调用 AI 生成一个文本块的局部剧本 YAML。局部结果包含：

- 该块涉及的人物候选
- 该块对应的场景
- beats
- warnings
- human_review_required

每块最多重试 2 次。如果仍失败，后端生成占位场景，写入 warnings 和 human_review_required，保证整体任务可以继续完成。

### 4. 合并 YAML

所有文本块完成后，由后端合并局部结果：

- `source` 使用原始章节列表。
- 人物按姓名优先去重，没有姓名时按 ID 去重。
- 场景按文本块顺序拼接。
- 场景 ID 重新编号。
- 场景人物引用映射到合并后的人物 ID。
- 汇总 warnings 和 human_review_required。
- 最后执行现有 Schema 修复与校验。

### 5. 进度展示

任务字段增加：

- `total_chunks`
- `completed_chunks`
- `current_chunk`

生成期间任务进度从 90% 到 99% 按块推进，阶段文案类似：

```text
正在处理第 2/5 个文本块：第二章
```

### 6. 任务列表摘要

任务列表接口只返回摘要字段，避免把大正文、YAML、章节正文直接塞进列表响应。详情接口仍返回完整任务信息。

### 7. 失败降级

单块 AI 失败时不直接让任务失败，而是生成占位场景：

- `beats[].type = note`
- `beats[].text` 说明该块生成失败，需要人工补充
- `quality_report.warnings` 记录失败块
- `quality_report.human_review_required` 标记人工复核

只有最终合并、序列化或任务基础结构彻底失败时，任务才进入 failed。

## PR 拆分建议

本次实现可作为一个稳定性修复 PR，也可以后续继续拆分：

- `feat: 支持长文本分块生成剧本 YAML`
- `feat: 展示 AI 分块生成进度`
- `fix: 降级处理单块 AI 生成失败`
- `refactor: 使用任务摘要接口优化任务列表`
- `feat: 持久化转换任务和中间结果`

## 后续演进

- 使用 PostgreSQL 持久化任务和分块结果。
- 增加失败块重试按钮。
- 增加全局摘要/人物表预处理阶段。
- 支持并发处理多个文本块，但需要控制供应商限流。
- 支持流式响应或异步任务回调。
